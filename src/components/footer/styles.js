import styled from 'styled-components'

export const Footer = styled.footer`
    padding: 1rem;
    display: flex; 
    justify-content: center;
    position: fixed;
    z-index: 10;
    bottom: 0;
    width: 100vw;
    background: white;
    visibility: hidden;
`