import ContentSection from "components/content-section"

const Contact = ({title}) => {
    return (
        <ContentSection>
            <p>{title}</p>
        </ContentSection>
    )
}

export default Contact